# 礼拜喵更新日志

## v2.5.0 — 本地 AI 陪伴预览版（2026-09-08）

这一版在 v2.4.1 原生 macOS 工程上扩展了四个模块：桌面聊天、每日新闻、桌宠行为、长期记忆与备忘检索。保留礼拜提醒、本地备忘、附近清真寺等原有功能。当前为工程预览版，尚未完成正式 App Store 发布验收。

### 1. 桌面里的本地 AI 聊天

**之前：** 从应用跳转 ChatGPT，或让用户填写自己的 OpenAI API key。

**现在：** 在桌面原生对话窗口里输入文字，礼拜喵使用本机模型回复。试用包内置 Qwen3-0.6B Q8 模型，安装后即可离线聊天。用户不需要 API key、第三方账号、Ollama、Node.js，也没有按次调用云端模型的费用。

- AppKit 原生聊天面板，支持调整窗口大小、浅色/深色外观、用户与猫的消息气泡。
- 支持逐步显示回复、停止生成、开始新对话；极短回答可能一次显示完成。
- 可选下载 Qwen3-1.7B Q8 模型，提供进度、取消、失败重试和删除；模型固定到官方提交并校验 SHA-256。
- llama.cpp 推理引擎编译在应用内，下载项是模型数据；采用 CPU 推理兼容两类 Mac 芯片。
- 删除旧 ChatGPT 网页聊天入口、OpenAI API 设置和请求逻辑；正式版本升级只尝试清除旧钥匙串条目，不读取旧密钥。
- 聊天与记忆不发送到模型云服务。新闻、地图、礼拜时间和可选模型下载仍会联网。

**边界：** 轻量模型可能答错、表达不自然，长对话和旧机器速度需要进一步验收；这不等同于商业云端大模型的能力。额外模型下载尚不支持跨重启断点续传。没有加入语音互动。

### 2. 每天首次打开时的新闻问候

用户在陪伴设置中启用新闻后，礼拜喵按所选城市、国家与所在地时区获取当天资讯，在对话框里展示最多三条。

- 优先配置中的本国官方来源；城市名称与标题匹配的内容优先，再考虑发布时间。
- 没有本国来源时使用现有国际专题来源。
- 每条保留原文标题、发布时间和原文链接，用户可核对出处。
- 过滤过期、未来、无日期、非法链接与重复条目，不用旧新闻补齐数量。
- 每日自动尝试标记持久化；退出后当天再次打开不会重复自动播报。
- 网络失败或当天无新闻时也不会在每次启动反复打扰，用户可手动重试。
- 后台跨过午夜不主动弹窗，下次激活应用时检查。
- 新闻默认关闭，由用户选择开启。

**已配置来源：** 英国 GOV.UK、新西兰 Beehive，以及英国外交部门和 NASA 的国际/科学专题资讯。联合国候选源默认禁用。

**边界：** 尚未覆盖每个国家和城市，也不能保证每天都有 2–3 条关键时事。国际专题源不是完整全球热点服务。标题保持来源语言，没有用模型生成未经核实的摘要。来源可公开访问不等于任意商用，扩展白名单前仍需核验条款。

### 3. 桌面猫的主动行为

新增独立透明桌宠窗口，通过菜单“到桌面散步”开启。双击猫可以进入聊天，右键可以选择动作或回到小窝。

| 行为 | 当前实现 |
| --- | --- |
| 看鼠标 | 根据鼠标相对位置调整朝向 |
| 扑鼠标 | 向鼠标所在方向扑跳 |
| 桌面奔跑 | 使用左右奔跑序列移动 |
| 趴窗边 | 读取公开窗口几何，在可用窗口边缘停留；不可用时回退桌面边缘 |
| 睡觉 | 进入安静的睡眠状态 |
| 被戳生气 | 点击互动触发短暂生气反馈 |
| 抚摸和求摸 | 响应抚摸互动，并间隔发出求摸状态 |
| 拖动 | 用户可以重新摆放桌面猫 |

同时提供安静模式和减少动态效果。没有读取屏幕截图、窗口标题或剪贴板，也不会代替用户点击其他应用。

**边界：** 复用原有 idle、waving、running-left、running-right 素材，加上位移、旋转和状态符号。独立的睡眠、趴卧、生气手绘帧仍需补齐；多显示器、Spaces、全屏和窗口最小化尚需完整实机验收。

### 4. Today Memory → Long-term Memory

记忆默认关闭。用户启用后，记录礼拜喵实际能观察到的应用内互动，并用可解释规则区分当日事件与长期信息。

- 今日记录：保留应用内互动、提醒反馈等真实事件，不把“未互动”解释成“没有礼拜”。
- 显式记忆：例如“记住：我喜欢轻提醒”，可以保存为长期事实。
- 行为归纳：重复模式需要至少三个不同日期的证据，避免一次行为就被贴上长期标签。
- 临时情绪：例如“今天有点累”不直接成为永久人格结论。
- 记忆可查看、编辑和清除；关闭记忆会取消当前生成并清空会话上下文。
- 本地 JSON 存储，当前 macOS 用户的数据独立保存；不上传聊天和记忆。不是应用级加密数据库。

**边界：** 不读取其他应用来判断用户是否正在工作；不会把用户提供的示例故事当成已发生的事实。长期归纳属于规则式实现，需要持续进行误记忆和遗忘体验验收。

### 5. 让聊天调用自己的备忘

- 保留原有 KnowledgeSpace 本地备忘功能。
- 聊天时可从应用内保存的 Markdown 备忘中检索相关片段。
- 支持中文双字与英文关键词匹配，保留文件名出处；限定读取大小与上下文片段，避免把全部文件塞进模型。
- 不自动扫描整台电脑，也不等于接入 Apple Notes。
- 清理聊天记忆与删除备忘是不同操作，关闭记忆不会偷偷删除用户的备忘文件。

### 6. 面向公开分发的工程变化

- 最低部署目标 macOS 13.0，构建 arm64 / x86_64 并合并 Universal 2。
- App Sandbox，仅使用网络客户端及原有位置权限；没有新增麦克风、录屏或辅助功能权限。
- 增加隐私清单、模型/推理库许可、新闻来源配置及发布门槛文档。
- 模型文件不写进 Git 历史；构建脚本可下载并校验，也可使用已有模型路径。
- GitHub 构建脚本在依赖缺失时获取固定提交的 llama.cpp；完整交付源码 ZIP 已包含该依赖。
- 本次试用包使用独立的 `com.yuqiqi.salahcat.preview` 标识，避免覆盖原正式应用的数据。

### 7. 已做验证与仍待完成

已通过 33 项核心检查、真实 0.6B 模型问答和取消测试、两架构编译、二进制 macOS 13 部署版本检查及临时签名验证。此前实际沙盒应用中观察到本地聊天回复和带日期/出处的当天新闻；检查了聊天组件的不同宽度与浅深色渲染。

Intel 当前只有交叉编译验证，没有 Intel 实机结论；macOS 13 最低版本字段不是 macOS 13 实机验收。还需完善低磁盘/断网/下载异常、长时间耗电、多显示器、记忆删除和完整本地化验收。新增界面中文/英文完整，阿语/乌尔都语目前回退英文。

正式上架还需要发行者证书、真实隐私/支持网址、素材和新闻使用权确认及 App Store Connect 审核配置。详见 [发布门槛](src/RELEASE-GATES.md) 与 [验证记录](docs/TEST-REPORT-v2.5.0.md)。

### 参考与致谢

交互方向参考 [Mochi](https://github.com/NatBrian/mochi-llm-pet)、[Miru](https://github.com/kiyotakali/Miru)、[Open-LLM-VTuber](https://github.com/Open-LLM-VTuber/Open-LLM-VTuber)。没有移植这些项目的角色素材、音频或 Python/Windows/Live2D 代码。实际推理依赖 llama.cpp 与 Qwen 官方模型，相关许可随源码提供。
